#' Check whether the student wrote a for loop correctly
#' 
#' @export
test_for_loop <- function(var, seq, sct_expr, undefined_msg, incorrect_msg) {
  
  test_that("write tests here", {
    
  })
}

#' Check whether the student wrote a while loop correctly
#' 
#' @export
test_while_loop <- function(var, seq, sct_expr, undefined_msg, incorrect_msg) {
  
  test_that("write tests here", {
    
  })
}